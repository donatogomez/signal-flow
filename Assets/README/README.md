# README assets

Screenshots and GIFs referenced by the root [`README.md`](../../README.md) live here.

Expected files (drop them in, then enable the matching image tags in the README's **Screenshots**
section):

| File | Shows |
| --- | --- |
| `dashboard.png` | Dashboard tab — fleet figures, status breakdown, recent events |
| `fleet.png` | Fleet list — search / sort / filter |
| `device-detail.png` | Device Detail — current telemetry + Swift Charts trends |
| `demo.gif` | Short end-to-end walkthrough (Dashboard → Fleet → Device Detail) |

Guidance:
- Capture on an iPhone simulator (e.g. iPhone 16 Pro) in light mode for crisp, neutral framing.
- Keep PNGs reasonably sized (≤ ~1.5 MB each); optimize the GIF (≤ ~8 MB).
- Use the simulated data source so captures are reproducible.
