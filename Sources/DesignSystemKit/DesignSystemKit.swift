/// DesignSystemKit — reusable SwiftUI components, design tokens, and status semantics.
///
/// Will hold the visual vocabulary shared by every feature (colors/tokens, typography,
/// status badges, gauges, chart styles). Independent of features and of the data layer.
///
/// Scaffolding placeholder — no components yet.
public enum DesignSystemKit {
    /// Marker confirming the module compiles and links. Removed once real components land.
    public static let moduleName = "DesignSystemKit"
}
