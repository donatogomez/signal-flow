/// FeatureFleet — the fleet overview surface.
///
/// Will hold the views, presentation models, and routes for browsing/filtering all monitored
/// devices and their derived status. Depends only on DomainKit and DesignSystemKit.
///
/// Scaffolding placeholder — no screens yet.
public enum FeatureFleet {
    /// Marker confirming the module compiles and links. Removed once real views land.
    public static let moduleName = "FeatureFleet"
}
