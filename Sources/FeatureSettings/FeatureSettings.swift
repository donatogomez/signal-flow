/// FeatureSettings — configuration surface.
///
/// Will hold the views, presentation models, and routes for thresholds, retention policy, units, and
/// data-source selection (live gateway vs. simulator). Depends only on DomainKit and DesignSystemKit.
///
/// Scaffolding placeholder — no screens yet.
public enum FeatureSettings {
    /// Marker confirming the module compiles and links. Removed once real views land.
    public static let moduleName = "FeatureSettings"
}
