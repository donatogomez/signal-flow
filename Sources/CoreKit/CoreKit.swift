/// CoreKit — shared, feature-agnostic foundations.
///
/// Will hold cross-cutting utilities that the rest of the package builds on:
/// injectable `Clock`, `AsyncSequence` helpers (debounce/throttle), cancellation scopes,
/// logging abstractions. Has **no** dependency on any other SignalFlow target.
///
/// Scaffolding placeholder — no logic yet.
public enum CoreKit {
    /// Marker confirming the module compiles and links. Removed once real types land.
    public static let moduleName = "CoreKit"
}
