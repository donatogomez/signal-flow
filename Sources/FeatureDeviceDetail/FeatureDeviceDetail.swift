/// FeatureDeviceDetail — a single device's live detail surface.
///
/// Will hold the views, presentation models, and routes for live metrics, freshness, and per-metric
/// drill-in for one device. Depends only on DomainKit and DesignSystemKit.
///
/// Scaffolding placeholder — no screens yet.
public enum FeatureDeviceDetail {
    /// Marker confirming the module compiles and links. Removed once real views land.
    public static let moduleName = "FeatureDeviceDetail"
}
