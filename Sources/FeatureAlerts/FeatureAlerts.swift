/// FeatureAlerts — the alert inbox surface.
///
/// Will hold the views, presentation models, and routes for listing, filtering, and acknowledging
/// alerts, plus notification routing. Depends only on DomainKit and DesignSystemKit.
///
/// Scaffolding placeholder — no screens yet.
public enum FeatureAlerts {
    /// Marker confirming the module compiles and links. Removed once real views land.
    public static let moduleName = "FeatureAlerts"
}
